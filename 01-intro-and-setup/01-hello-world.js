// Ctrl + C để sao chép, Ctrl + V để dán, Ctrl + S để lưu file.
// Cài extension cần thiết để hỗ trợ viết và chạy code.
// Để lưu file, nhấn Ctrl + S.

// In ra dòng chữ chào các bạn trong terminal.
console.log("xin chào các bạn");
// In ra dòng chữ chào các bạn2 trong terminal.
console.log("xin chào các bạn2");

// Đường dẫn thư mục gốc của dự án:
// E:\playwright-pro\202603-PW-JS-TS\

// Tên file hiện tại:
// 01-intro-and-setup\01-hello-world.js

// Lệnh `cd` dùng để di chuyển giữa các thư mục trong terminal.
// Khi mở terminal, thư mục hiện tại thường là thư mục gốc của dự án.
// Gõ `cd` rồi nhấn `Tab` để tự động hoàn thành tên thư mục cần đi vào.
// `ls` = list, dùng để hiển thị các file và thư mục trong thư mục hiện tại.
// Gõ `node <ten-file>.js` để chạy file JavaScript.
// Extension đã thiết lập sẵn môi trường Node.js để có thể chạy file.

// Ctrl + / dùng để comment hoặc bỏ comment code.
